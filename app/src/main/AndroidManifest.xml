<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.areeb.supertextiles">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuperTextiles">
        <activity android:name=".EditCustomerActivity" android:windowSoftInputMode="stateVisible"/>
        <activity android:name=".ViewCustomerActivity" />
        <activity android:name=".AddCustomerActivity" android:windowSoftInputMode="stateVisible" />
        <activity android:name=".EditReportActivity" />
        <activity android:name=".ViewReportActivity" />
        <activity android:name=".EditBillActivity" />
        <activity android:name=".ViewBillActivity" />
        <activity android:name=".ChallanDetailsActivity" />
        <activity
            android:name=".AddMetersActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".AddChallanActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity android:name=".AboutUs" />
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="stateAlwaysVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateVisible" />
    </application>

</manifest>